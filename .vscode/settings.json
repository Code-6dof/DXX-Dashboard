{
  "VsCodeTaskButtons.showCounter": true,
  "VsCodeTaskButtons.tasks": [
    {
      "label": "$(rocket) Deploy",
      "task": "🚀 Deploy",
      "tooltip": "Git push + sync to VM + restart services"
    }
  ]
}
